[project]
name = "ccn-docs"
version = "0.1.0"
description = "Processes and policies for the Conference on Cognitive Computational Neuroscience."
requires-python = ">=3.13"
dependencies = [
    # >=0.0.40 is required for native macros support (Jinja variables/includes).
    "zensical>=0.0.40",
]

[tool.uv]
package = false
